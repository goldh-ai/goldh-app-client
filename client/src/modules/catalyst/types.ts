/**
 * Catalyst Intelligence Engine — Client-Side Types
 *
 * Re-exports schema types from @shared/schema for use within the catalyst module.
 */

export type { MacroEvent, EarningsEvent, CatalystEvent } from '@shared/types';
export type { MacroEventName } from '@shared/types';
